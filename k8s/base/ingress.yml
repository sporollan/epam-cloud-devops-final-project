apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fastapi-ingress
spec:
  rules:
    - host: PLACEHOLDER_HOST
      http:
        paths:
          - path: /greeting
            pathType: Prefix
            backend:
              service:
                name: fastapi-service
                port:
                  number: 80